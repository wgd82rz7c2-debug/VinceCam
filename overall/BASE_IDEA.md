# Base idea

> The source of truth for what VinceCam is meant to be. Every module under `src/`
> (`resume-extract/`, `preferences/`, `enjoyment/`, `trajectory/`, `scoring-engine/`,
> `job-logic/`) should trace back to something written here. A future validation agent
> checks that each module's supporting info (its own docs, code comments, whatever it
> claims to do) doesn't drift from this file.

_(Not yet established. Fill in: what problem VinceCam solves, who it's for, and how the
six modules fit together to solve it.)_

## Core concept

_TBD_

## How the modules serve the core concept

| Module | Role in the base idea |
| --- | --- |
| `resume-extract` | _TBD_ |
| `preferences` | _TBD_ |
| `enjoyment` | _TBD_ |
| `trajectory` | _TBD_ |
| `scoring-engine` | _TBD_ |
| `job-logic` | _TBD_ |

## Validation

_Not yet built. Intent: an agent that reads this file plus each module's supporting
docs/code, and flags where a module's stated purpose or behavior no longer matches what's
written here._
